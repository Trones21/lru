[workspace]
resolver = "2"
members = [
    "attempt-1",
    "attempt-2",
    "attempt-3",
    "attempt-4",
]

# post-01/ is deliberately NOT a workspace member: its Rust program is meant to
# fail to compile (that's the lesson), and a non-compiling member would break
# `cargo build` for the whole workspace.
