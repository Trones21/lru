[package]
name = "attempt-3"
version = "0.1.0"
edition = "2021"

[dependencies]

# Part 6 benchmarks attempt-3 against attempt-2. Add criterion when you get there:
# [dev-dependencies]
# criterion = "0.5"
#
# [[bench]]
# name = "lru"
# harness = false
